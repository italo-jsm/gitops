apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: public-certificates
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/italo-jsm/gitops.git
    targetRevision: HEAD
    path: infrastructure/public-certificates

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
